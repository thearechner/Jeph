// A $( document ).ready() block . jQuery
$( document ).ready(function() {
  // All other jQuery code goes inside here
   // $(window).scroll(function () {
   //    if ($(this).scrollTop() > 50) {
   //       $(‘body’).addClass(‘changeColor’)
   //    }
   //    if ($(this).scrollTop() < 50) {
   //       $(‘body’).removeClass(‘changeColor’)
   //    }
   //  });
});
